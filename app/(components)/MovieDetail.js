import React from 'react';
import MovieDetails from "@/app/movies/[movieId]/page";

const MovieDetail = () => {
    return (
        <div>
            <MovieDetails/>
        </div>
    );
};

export default MovieDetail;